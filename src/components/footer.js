import React from "react"

import "./concept.css"

const Footer = () => (
    <footer 
        style={{
            backgroundColor: 'var(--dark-grey)',
            color: 'var(--white)',
            font: '0.8rem',
            textAlign: 'center',
            padding: '0.5rem',
            position: 'absolute',
            bottom: 0,
            left:0,
            right:0
        }}
    >
        A TUNAFE @ Desde 1992
    </footer>
)

export default Footer;